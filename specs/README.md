# `specs/` 规格目录（清理后）

本目录中的 Hamel 算法规格为**本地-only** 文件（见根目录 `.gitignore`）：

- `hamel_checked_algorithm_spec.md` — 从 Hamel 论文提取，需自行放置

说明：
- 旧规格文件（`00`–`04`、`species.md`）已从本目录清理，避免继续混入未完成核验或相互冲突的算法描述。
- 同构级差异与整改项以本地 `hamel_checked_algorithm_spec.md` 为规格入口；源码注释仍引用 `docs/hamel_submodels/`（同为本地-only）。
