# Hamel 子模型分册（最终核验基线）

本目录用于严格复现 Hamel 原版论文（PDF）中的算法方程、参数与编号体系。

## 核验基线

- 主源：`docs/_2001_VDI-Dis._Mathematische Modellierung und experimentelle Untersuchung der Vergasung verschiedener fester Brennstoffe.pdf`
- 辅助提取：`data/hamel_1999_extracted.txt`
- 图像复核：`data/hamel_pages_png/page_{25,26,27,30,31,33,34,36,37,57,58}.png`

## 强制规则

1. 仅使用论文原编号（如 `(3.41)`, `(5.47)`, `(A.27)`）。
2. 仅记录论文原式或可直接对应的原文片段；禁止论文外推导补写。
3. 所有参数单位、符号、指数形态以原文为准；如存在识别歧义，必须显式标注。
4. 文档中的“实现说明”仅限论文原文可支撑内容，不引入工程化伪代码。

## 分册清单

- `01_global_nr_and_connectivity.md`
- `02_cell_balances_and_exchange.md`
- `03_hydrodynamics_core_chain.md`
- `04_freeboard_particle_trajectory_analytic.md`
- `05_drying_and_daem_pyrolysis.md`
- `06_kinetics_r1_r11_and_equilibrium_driving.md`
- `07_appendix_a1_gibbs_solver.md`

## 编号-页锚点核对表（最终）

### 01 全局 Newton-Raphson 与连接矩阵

- `(2.9)` -> `PAGE 20` -> `可直接采信`
- `blocktridiagonale Struktur / Nebenelemente` -> `PAGE 20` -> `可直接采信`

### 02 单元守恒与相间交换

- `(2-4)` -> `PAGE 18` -> `可直接采信`
- `(2-5)` -> `PAGE 18` -> `可直接采信`
- `(2-6)` -> `PAGE 18` -> `可直接采信`
- `(2-7)` -> `PAGE 19` -> `可直接采信`
- `(2.8)` -> `PAGE 19` -> `可直接采信`

### 03 床层水力学主链

- `(3.23)` -> `PAGE 22` -> `可直接采信`
- `(3.41)` -> `PAGE 25` -> `可直接采信（五次高分辨率裁图确认：指数 (P_0/p)）`
- `(3.42)` -> `PAGE 25` -> `可直接采信`
- `(3.43)` -> `PAGE 25` -> `可直接采信`
- `(3.44)` -> `PAGE 26` -> `可直接采信`
- `(3.46)` / `(3.47)` / `(3.48)` / `(3.49)` / `(3-50)` -> `PAGE 27` -> `可直接采信`

### 04 Freeboard 颗粒轨迹

- `(3.70)` / `(3.71)` / `(3.74)` / `(3.75)` / `(3.76)` -> `PAGE 30~31` -> `可直接采信`
- `(3.85)` / `(3.88)` / `(3.89)` / `(3.90)` / `(3.91)` / `(3.92)` -> `PAGE 32~33` -> `可直接采信`
- `(3.93)`~`(3.105)` -> `PAGE 33~34` -> `可直接采信`

### 05 干燥与热解

- `(4.1)` / `(4.2)` / `(4.3)` / `(4.4)` / `(4.9)` / `(4.10)` / `(4.11)` -> `PAGE 36` -> `可直接采信`
- `(4-12)` -> `PAGE 37` -> `可直接采信`
- `Tabelle 4.1` -> `PAGE 37` -> `可直接采信`

### 06 动力学与平衡驱动力

- `(5.45)` -> `PAGE 57` -> `可直接采信（五次高分辨率裁图逐字符确认）`
- `(5.46)` / `(5.47)` / `(5.48)` / `(5.49)` / `(5.51)` -> `PAGE 57` -> `可直接采信`
- `(5.52)`~`(5.57)` / `(5.59)` / `(5.60)` / `(5.61)` -> `PAGE 58` -> `可直接采信`
- `Tabelle 5.4` -> `PAGE 58` -> `可直接采信`

### 07 附录 A1 气相平衡

- `(A.1)` / `(A.2)` / `(A.3)` -> `PAGE 91` -> `可直接采信`
- `(A.18)` / `(A.20)` -> `PAGE 92~93` -> `可直接采信`
- `(A.27)` / `(A.28)` / `(A.30)` / `(A.34)` -> `PAGE 93` -> `可直接采信`
- `(A.35)`~`(A.40)` -> `PAGE 94` -> `可直接采信`

## 当前状态

- 高优先级与低优先级 `OCR断裂` 项均已清零。
- 00~07 文档当前以“最终核验基线”为准。
- 早期轮次过程性记录（layout / fitz / word-level）已折叠，不再作为当前状态判定依据。

## 手动核对项

- 无（已全部完成高分辨率逐字符复核）。

## 修改标注（2026-05-07）

- `[M00-1]` 更正页锚点：`Tabelle 4.1` 从 `PAGE 55` 修正为 `PAGE 37`（与 `data/hamel_1999_extracted.txt` 中 `===== PAGE 37 =====` 段落一致）。
